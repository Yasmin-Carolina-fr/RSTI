const readline = require('readline');
const rl = readline.createInterface({
  input: process.stdin,
  output: process.stdout
});
interface.question('Informe seu nome:', (nome) => {
  let nota1 = parseInt(nota1);
  let nota2 = parseInt(nota2);
  if (parseInt(nota1) + parseInt(nota2) / 3) {
    const meida = parseInt(media);
    console.log(media);

    console.log(`sua media é ${media} um otimo aproveitamento.`);
  }
});
